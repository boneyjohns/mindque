import 'package:flutter/material.dart';

class Loginpageanimation extends StatelessWidget {
  const Loginpageanimation({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Image.asset(
        'lib/asset/111594-login.gif',
        height: 220,
      ),
    );
  }
}
